# Extracting a window from a XYZ file

cropxyz.c is a C program extracting a window from an XYZ file (used by FullSWOF_2D),
i.e. extract a subset of rectangular shape.

To get a description of the command-line inputs, simply launch the executable.
